# Obscuron — Python dependencies
# Audit-only workflow: forward inference on pretrained models + lightweight statistical modelling.
# No model training required; runs on M1 Pro (MPS) or RTX 4060 (8GB).

# Core numerics / data
numpy
scipy                # EVT tail fits: scipy.stats.genpareto, genextreme, weibull_min
pandas
scikit-learn         # novelty baselines: kNN, IsolationForest, LocalOutlierFactor, OneClassSVM; AUROC

# Embeddings (forward inference)
torch
transformers         # ESM-2 (facebook/esm2_*) and genomic foundation models via HF
fair-esm             # ESM-2 / ESMFold reference implementation
# genos-m: install per its preprint release instructions once verified (see ACKNOWLEDGEMENTS)

# Sequence / bio I/O
biopython            # FASTA parsing, translation, sequence handling

# Nearest-neighbour search at scale (boundary-of-known distances)
faiss-cpu

# Config / experiment plumbing
pyyaml
tqdm

# Figures
matplotlib
seaborn

# Notebooks
jupyter
