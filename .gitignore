# Reference papers — copyright; cite in ACKNOWLEDGEMENTS.md instead
Paper/

# Reference-database snapshots, sequences, embeddings — never commit (large; download/build via scripts)
# includes GTDB releases, Pfam/UniProt dumps, FASTA, and cached embedding matrices.
# Small derived artifacts (manifest, summary stats, sampled panels) ARE committed —
# see docs/reproducibility.md and Track1_phase1_benchmark_scope.md section 6 ("the
# committed artifact; sequences and embeddings stay gitignored") — so this ignores
# data/ contents rather than the directory itself, with explicit un-ignores below.
/data/*
!/data/manifest.json
!/data/.ops/
!/data/processed/
/data/processed/*
!/data/processed/*/
/data/processed/*/*
!/data/processed/*/*.json
!/data/processed/*/*.csv
/data/processed/*/taxonomy.csv

# External tools cloned/built locally, not vendored (e.g. Foldseek binaries) — see README/ACKNOWLEDGEMENTS
tools/

# Model checkpoints, cached embeddings, and run outputs
checkpoints/
embeddings/
outputs/
runs/
models_cache/
.cache/

# Weights & Biases
wandb/

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
dist/
build/
*.so
uv.lock.bak

# Jupyter
.ipynb_checkpoints/

# Environment
.env
*.env
.venv/

# OS / editor
.DS_Store
.vscode/
.idea/

# Large binary formats — should live under data/ anyway, never in git
*.fasta
*.faa
*.fna
*.fa
*.h5
*.hdf5
*.pt
*.npy
*.npz
*.faiss
*.pkl
*.hmm

# Reference documents (large binary, not source)
Illuminating_Microbial_Dark_Matter_Blueprint.docx

# Figures generated by scripts — regenerable, not source
figures/*.png
figures/*.pdf
figures/*.svg

# Nemotr
Run NVIDIA Nemotron 3 Ultra FREE in Your Terminal.docx

# Dark Matter Files
Microbial_Dark_Matter_Blueprint_Updated.pdf
Microbial_Dark_Matter_Roadmap_Cards.docx